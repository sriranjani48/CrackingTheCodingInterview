
public class Solution {
	
	public void replaceSpace(String inputStr) {
		char[] outputStrArray = inputStr.toCharArray();
		for(int i = 0; i < outputStrArray.length(); i++) {
			if(inputStr.charAt(i) == ' ') {
				
			}
		}
	}

}
