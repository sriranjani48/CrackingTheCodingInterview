
public class Solution {
	
	public void replaceSpace(String inputStr) {
		char[] outputStrArray = new char[inputStr.length()];
		for(int i = 0; i < inputStr.length(); i++) {
			if(inputStr.charAt(i) == ' ') {
				inputStr[i] = "";
			}
		}
	}

}
